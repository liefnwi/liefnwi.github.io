source "https://rubygems.org"

# Use GitHub Pages official gem to match its build environment
gem "github-pages", group: :jekyll_plugins

# Optional: these are used only in local development
group :jekyll_plugins do
  # No need to add jekyll-feed or minima separately — github-pages includes them
end

# # Optional gems for local development only
# platforms :mingw, :x64_mingw, :mswin, :jruby do
#   gem "tzinfo", "~> 1.2"
#   gem "tzinfo-data"
#   gem "http_parser.rb", "~> 0.6.0", :platforms => [:jruby]
# end

# gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]

# # Optional dev/test tools (not used by GitHub Pages)
# group :development do
#   gem "webrick", "~> 1.7"
#   gem "rake"
#   gem "html-proofer"
# end